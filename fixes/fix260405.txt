2026.04.05
fix: исправление поиска установщиков и добавление скриптов управления политикой выполнения

ИСПРАВЛЕНИЯ В Common-Functions.ps1:
- Добавлено правильное объявление функции Expand-Archive (была сломана)
- Исправлен Save-FileWithFallback: теперь ВСЕГДА скачивает заново (удаляет старый файл)
- Добавлена проверка, что скачанный файл не пустой

ИСПРАВЛЕНИЯ В Install-AvestComponents.ps1:
- Исправлен поиск установщиков для AvCSPBel и AvCSPBign
  - Поиск в AvPKISetup(*)\data\setupAvCSP*.exe
  - Поиск в корне unpacked\setupAvCSP*.exe (отдельный архив)
- Убрано использование Join-Path с подстановочными знаками *
- Теперь используется Get-ChildItem с параметром -Filter

ИСПРАВЛЕНИЯ В Install-AvCMXWebP.ps1:
- Исправлен поиск установщика плагина Find-PluginInstaller
  - Поиск в AvPKISetup(bel)\data\AvCMXWebP-*.exe
  - Поиск в AvPKISetup(bign)\data\AvCMXWebP-*.exe
- Убрано использование Join-Path с подстановочными знаками *
- Добавлены резервные URL для скачивания (FallbackURL1, FallbackURL2)
- Выбор самого свежего установщика при наличии нескольких версий

ДОБАВЛЕНЫ НОВЫЕ СКРИПТЫ:

1. enable_scripts.ps1 — Разрешение выполнения PowerShell сценариев
   - Устанавливает политику RemoteSigned
   - Разблокирует все .ps1 файлы в папке (Unblock-File)
   - Поддерживает установку через реестр (обход ошибок)
   - Полностью на русском языке

2. disable_scripts.ps1 — Запрет выполнения PowerShell сценариев
   - Устанавливает политику Restricted
   - Требует подтверждения перед выполнением
   - Полностью на русском языке


ТЕПЕРЬ:
- Поиск установщиков работает без ошибок Join-Path
- Криптопровайдеры и плагин находятся корректно
- Скачивание всегда свежих версий (старые удаляются)
- Управление политикой выполнения через удобные скрипты
- Задача в планировщике и GUI работают даже при Restricted (используют -ExecutionPolicy Bypass)